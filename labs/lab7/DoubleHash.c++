#
 class doubleHash {


       Int table_size, keysPresent, PRIME;
       vector<int> hashTable;
      bitset<




public:
  doublehash 








Int main(){


// to create an empty hash table of size 13
Int insert[] = { 16, 1, 13, 14};
Int n = sizeof(insert)/sizeof(insert[0]);


//inserting keys into the has table
Double h;
for(int i = 0; i < n; i++)
    h.insertHash(insert[i]);
  Std::cout <<